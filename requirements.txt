# Core training stack. Install torch separately with the build matching your CUDA
# version: https://pytorch.org/get-started/locally/
torch>=2.4.0
tokenizers>=0.20.0,<1.0.0
numpy>=1.26.0,<3.0.0
tqdm>=4.66.0,<5.0.0

# Optional, only for the dataset download helpers in scripts/
datasets>=2.19.0
requests>=2.31.0

# Optional, only for multimodal training and inference (geocentric train-vision)
pillow>=10.0.0
